ansible all -m ping

1
ansible debian -m ansible.builtin.apt -a "name=apache2 state=present update_cache=yes"

2
ansible redhat -m ansible.builtin.yum -a "name=httpd state=present"

3
ansible debian -m ansible.builtin.copy -a "src=./index.html dest=/var/www/html/index.html"

4
ansible debian -m ansible.builtin.file -a "path=/tmp/testdir state=directory mode=0755"

5
ansible redhat -m ansible.builtin.copy -a "content='Hello World' dest=/tmp/hello.txt"

6
ansible debian -m ansible.builtin.service -a "name=apache2 state=started"
ansible redhat -m ansible.builtin.service -a "name=httpd state=started"

7
ansible debian -m ansible.builtin.apt -a "name=git state=present update_cache=yes"

8
ansible redhat -m ansible.builtin.yum -a "name=git state=present"

9
ansible debian -m ansible.builtin.user -a "name=testuser state=present"
ansible redhat -m ansible.builtin.user -a "name=testuser state=present"

10
ansible all -m ansible.builtin.setup
